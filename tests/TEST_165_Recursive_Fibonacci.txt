int fib(int n)
{
    if (n < 2)
    {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

void main()
{
    PrintInt(fib(0));
    PrintInt(fib(1));
    PrintInt(fib(2));
    PrintInt(fib(5));
    PrintInt(fib(10));
    PrintInt(fib(15));
}
