string repeat(string s, int n)
{
    string result := "";
    while (n > 0)
    {
        result := result + s;
        n := n - 1;
    }
    return result;
}

void main()
{
    PrintString(repeat("ab", 3));
    PrintString(repeat("x", 5));
}
