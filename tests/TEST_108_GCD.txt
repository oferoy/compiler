int gcd(int a, int b)
{
    while (b > 0)
    {
        int t := b;
        b := a - (a / b) * b;
        a := t;
    }
    return a;
}

void main()
{
    PrintInt(gcd(48, 18));
    PrintInt(gcd(100, 75));
    PrintInt(gcd(7, 13));
}
